Global setup:

  git config --global user.email misieks@gmail.com
      

Next steps:

  mkdir foo
  cd foo
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:misiek/foo.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:misiek/foo.git
  git push origin master
      

